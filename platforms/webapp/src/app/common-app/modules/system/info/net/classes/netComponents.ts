import { NetCardComponent, FcCardComponent, TeamComponent, BridgeComponent } from '../modules';

const netComponents = {
    net: NetCardComponent,
    fc: FcCardComponent,
    team: TeamComponent,
    bridge: BridgeComponent
};

export { netComponents };
