import { TiFileInfo, TiFileItem, TiUploadConfig, TiUploadRef } from './TiFileInterface';
/**
 * @ignore
 */
export declare class TiFileItemUtil {
    /**
     * 创建文件对象，该返回值用作后续文件的上传和文件操作，可作为文件回调参数传递
     * fileObject 文件信息对象
     * fileOrInpt 原始文件对象 H5下为file对象，非H5下为fileInput
     * config 文件上传配置信息
     * uploader 上传文件对应的文件实例
     * return 文件对象
     */
    static createFileItem(tifileObject: TiFileInfo, fileOrInput: Element | File, config: TiUploadConfig, uploader: TiUploadRef): TiFileItem;
    /**
     * 创建文件对象，统一H5和非H5情况下的文件信息
     * file {FileList|FileInput} 原始文件对象 H5为file对象形式，非H5为fileinput对象
     * return 类文件对象
     */
    static createFileObject(fileThis: any): TiFileInfo;
    /**
     * 将文件大小显示标准化：根据文件大小做不同单位的显示，文件大小保留两位小数
     * 文件大小
     * 带单位文件大小
     */
    private static formatSize;
}
