import { TiFileItem } from './TiFileInterface';
/**
 * @ignore
 */
export declare class TiUploadUtil {
    static readonly isHTML5: boolean;
    /**
     * 上传多个文件
     * 上传一个或多个文件item对象
     * 无
     */
    static uploadItems(items: Array<TiFileItem>): void;
    /**
     * 取消队列中多个正在上传中的文件(只在上传时可取消)
     * 文件item对象数组
     * 无
     */
    static cancelItems(items: Array<TiFileItem>): void;
    /**
     * 删除队列中单个或多个上传文件
     * 文件item对象数组
     */
    static removeItems(items: Array<TiFileItem>): void;
    /**
     * 触发onBeforeRemoveItems事件
     * 文件item对象数组
     */
    static onBeforeRemove(items: Array<TiFileItem>): void;
    /**
     * 获取文件对象 index值，获取该值用于后续判断item有效性
     * 上传文件item对象
     */
    private static getItemIndex;
    /**
     * 判断文件是否为有效文件
     * 上传文件item对象
     */
    private static isValidFileItem;
    private static generateUploadData;
    private static setXhr;
    private static setheaders;
    /**
     * xhr方式上传文件
     * 上传一个或多个文件item对象，上传单个文件的情况下
     */
    private static uploadXhr;
    private static uploadForm;
    private static isSuccessCode;
    /**
     * 上传前处理
     * 上传一个或多个文件item对象
     */
    private static onBeforeSend;
    private static handleEvent;
    private static onProgress;
    private static onSuccess;
    private static onError;
    private static onComplete;
    private static onCancel;
    private static onRemove;
}
