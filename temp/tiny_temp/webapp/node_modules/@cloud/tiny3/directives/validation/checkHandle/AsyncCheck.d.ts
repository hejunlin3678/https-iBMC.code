import { CheckHandle } from './CheckHandle';
import { NgControl } from '@angular/forms';
import { ElementRef } from '@angular/core';
/**
 * @ignore
 *
 * 10.1.0新增
 */
export declare class AsyncCheck extends CheckHandle {
    onStatusChange(ele: ElementRef, formControl: NgControl): void;
    onAsyncStatusChange(ele: ElementRef, formControl: NgControl): void;
    addPendingIcon(ele: ElementRef, formControl: NgControl): void;
    clearPendingIcon(ele: ElementRef): void;
    private getPendingStateRef;
}
