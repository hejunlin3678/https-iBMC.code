import { BlurCheck } from './BlurCheck';
import { CheckHandle } from './CheckHandle';
import { TiValidationConfig } from '../TiValidationInterface';
import { NgControl } from '@angular/forms';
import { ElementRef } from '@angular/core';
/**
 * @ignore
 */
export declare class PwdCheck extends BlurCheck implements CheckHandle {
    private config;
    private common;
    private msgItems;
    private validator;
    private levelFn;
    constructor(config: TiValidationConfig, common: any);
    onFocus(ele: ElementRef, formControl: NgControl): void;
    private _setValidator;
    private _setMsgItems;
    private _setLevelFn;
    addTip(ele: ElementRef, validationConf: TiValidationConfig, formControl: NgControl): void;
}
