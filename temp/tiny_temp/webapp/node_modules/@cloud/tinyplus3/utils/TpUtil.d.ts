/**
 * @ignore
 * 工具类
 */
export declare class TpUtil {
    static setAttrs(element: HTMLElement, attrs: object): void;
    static isObject(item: any): boolean;
    static extend(target: object, source: object): object;
    /**
     * url 跳转的目标路径 string
     * params  参数对象集合 Object
     */
    static addUrlParams(url: string, params: any): string;
    /**
     * url 跳转的目标路径 string
     * params 参数对象集合 Object
     */
    static addDefaultParam(url: string, params: any): string;
    /**
     * 得到cookie的值
     * @param key： cookie名对应的键值
     */
    static getCookie(key: string): any;
    /**
     * 设置cookie的值，包括key、value
     */
    static setCookie(key: string, value: string): void;
}
