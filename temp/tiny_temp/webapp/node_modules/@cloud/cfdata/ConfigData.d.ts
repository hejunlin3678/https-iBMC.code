import { CfLang } from './interface/CfLang';
import { CfMenu } from './interface/CfMenu';
import { CfUi } from './interface/CfUi';
/**
 * 对外提供的唯一数据类，导入此模块即可使用。
 * 注意：项目构建要执行局点配置的
 * @export
 * @class ConfigData
 */
export declare class ConfigData {
    /**
     * 局点配置语言数据
     * 局点数据，不经过网络，所以是同步获取。
     * @static
     * @returns {CfLang}
     * @memberof CfData
     */
    static getLangConfig(): CfLang;
    /**
     * 局点配置菜单数据
     *
     * @static
     * @returns {CfMenu}
     * @memberof CfData
     */
    static getMenuConfig(): CfMenu;
    /**
     * 局点配置UI数据
     *
     * @static
     * @returns {CfUi}
     * @memberof CfData
     */
    static getUiSwitchConfig(): CfUi;
}
