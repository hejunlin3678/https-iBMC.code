import { CfEndpoints } from './interface/CfEndpoints';
export declare class EndpointsData {
    private static cfEndpoints;
    private static EndpointsPromise;
    /**
     * Gets cf Endpoint
     * @returns cf Endpoint
     */
    static getCfEndpoints(projectName?: string, isReFresh?: boolean): Promise<CfEndpoints>;
    static getCfEndpointsSync(): CfEndpoints;
    private static initEndpoints;
    private static dealFederatedEndpoints;
    private static initDealEndpoints;
    private static dealAllEndpoints;
    private static beautifierEndpoints;
    /**
     * 针对数组装简单对象，仅两层深copy
     * Arrays copy
     * @param arr
     * @returns copy
     */
    private static copy;
}
