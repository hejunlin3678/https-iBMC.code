import { ApiService } from './ApiService';
import { User } from '../interface/User';
/**
 *
 *
 * @export
 * @class UserSerivce
 * @extends {ApiService}
 */
export declare class UserSerivce extends ApiService {
    /**
     * 获取登录用户信息
     *
     * @param  isRefreshToken
     * @returns
     * @memberof UserSerivce
     */
    private static meDataBak;
    static getLoginUser(isRefreshToken?: boolean): Promise<User>;
    /**
     * @internal
     * 貌似内外都没有用到
     * Gets user choices
     * @param options
     * @returns user choices
     */
    static getUserChoices(options: any): Promise<any>;
    /**
     * @internal
     * 貌似内外都没有用到
     * Updates user choices
     * @param options
     * @returns user choices
     */
    static updateUserChoices(options: any): Promise<any>;
    /**
     * 查询AssumeRoles
     *
     * @param  options
     * @returns
     * @memberof UserSerivce
     */
    static getAssumeRoles(options: any): Promise<any>;
    /**
     * @internal
     * 貌似内外都没有用到
     * Assumes role
     * @param options
     * @returns role
     */
    static assumeRole(options: any): Promise<any>;
    static getUserProjects(options: any): Promise<any>;
    static queryIamUser(userId: any): Promise<any>;
    /**
     * @internal
     * 貌似内外都没有用到
     * Querys mc user role
     * @param userId
     * @returns mc user role
     */
    static queryMcUserRole(userId: string): Promise<any>;
}
