import { HttpConfig } from '../interface/HttpConfig';
export declare class HttpService {
    static get(config: HttpConfig): Promise<any>;
    static delete(config: HttpConfig): Promise<any>;
    static put(config: HttpConfig): Promise<any>;
    static post(config: HttpConfig): Promise<any>;
    static patch(config: HttpConfig): Promise<any>;
    static fetch(config: HttpConfig): Promise<any>;
}
