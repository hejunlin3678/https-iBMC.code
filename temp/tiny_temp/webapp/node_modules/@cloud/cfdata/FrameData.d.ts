import { CfFrame } from './interface/CfFrame';
export declare class FrameData {
    private static cfFrame;
    /**
     * 获取和修改数据，都用这个接口.
     * 因为获取的数据跟局点配置相关，所以是同步接口。
     * Gets cf frame
     * @returns cf frame
     */
    static getCfFrame(): CfFrame;
    static getAppWebPath(): string;
    static getLanguage(key?: string): string;
    private static languageTranslate;
}
