import { CfLang } from "./CfLang";
import { CfMenu } from "./CfMenu";
import { CfUi } from "./CfUi";
/**
 * 内部接口，不对外。定义了window上挂的数据类型
 * @internal
 * @export
 * @interface CfMenu
 */
export interface CfWindow {
    cfLang: CfLang;
    cfMenu: CfMenu;
    cfUi: CfUi;
}
